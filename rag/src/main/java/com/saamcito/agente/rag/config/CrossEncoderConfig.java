package com.saamcito.agente.rag.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
// Asumiendo que el starter 'spring-ai-transformers' está presente
// Importamos la clase (si existiese) o creamos nuestra implementación custom compatible.

@Configuration
public class CrossEncoderConfig {

    // Reservado para la configuración del Bean ONNX en el futuro cercano,
    // o para instanciar la clase si está en el classpath.
}
